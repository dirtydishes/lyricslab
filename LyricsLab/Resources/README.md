# Resources

Drop large, bundled app resources here.

## CMU Pronouncing Dictionary

- Put `cmudict.txt` in this folder.
- Ensure the file is added to the `LyricsLab` target (Copy Bundle Resources).
- The app will look for `cmudict.txt` (also supports `cmudict-0.7b.txt`) in the main bundle.
